<!--非同期の通信がremote: trueで実行可能（Ajax通信）-->
<%= form_for(current_user.active_relationships.build) do |f| %>
  <div><%= hidden_field_tag :followed_id, @user.id %></div>
  <%= f.submit "フォローする", class: "btn btn-primary", id: "unfollow" %>
<% end %>